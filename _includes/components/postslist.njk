<section class="grid-3-columns" >
	{% for post in postslist | reverse %}
		<div class="postwrapper">
		<article{% if post.url == url %} data-current="current item"{% endif %}>
			
			<h3>
				<a href="{{ post.url | url }}">
					{% if post.data.title %}
						{{ post.data.title }}
					{% else %}
						Untitled
					{% endif %}
				</a>
			</h3>
{% if post.data.title %}
<div class="blogsumimage">
<a href="{{ post.url }}"><img src="{{ post.data.image }}/tr:w-400,h-300" alt="{{ post.data.imageAlt }}" title="{{ post.data.imageAlt }}" class="snippet__image"></a></div>
{% endif %}


			{% if post.data.description %}
				<p>
					{{ post.data.description }}
				</p>
			{% endif %}
			<p>
				<time datetime="{{ post.date | machineDate }}">
					<small>{{ post.date | readableDate }} by <a href="/authors/{{ post.data.author | slug }}/">{{ post.data.author }}</a></small>
				</time>
			</p>
			{% if post.data.tags %}
				<p>
					{% for tag in post.data.tags %}
		  			{%- if tag != "post" -%}
		  				{% set tagUrl %}/tags/{{ tag }}/{% endset %}
		  			<a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
		  			{%- endif -%}
					{% endfor %}
				</p>
			{% endif %}
		</article>
		</div>
	{% endfor %}
</section>


<!-- Start News Display Template -->
{if $items|@count > 0}
    <div class="grid-3-columns">
<!-- items -->
{foreach from=$items item=item}
	<!-- item -->
<div class="NewsSummaryHome">
<div class="CGBlogSummaryField">
<a href="{$item->url}" title="{$item->title}">
<img class="lazy" src="https://ik.imagekit.io/webtactics/gtmassage/{$item->image}?tr=w-450,h-270" alt="{$item->title}" title="{$item->title}"/>
<span class="image-overlay">
<span class="overlay-icon"><img src="{root_url}/uploads/images/assets/summicon.png" /></span></span></a>
</div>
<div class="NewsSummaryLinkHome">
<h2><a href="{$item->url}" title="{$item->title|cms_escape:htmlall}">{$item->title|cms_escape}</a></h2>
</div>
<div class="NewsSummaryPrice">   
{if !empty($item->Price30)}30 min: {$item->Price30} | {/if}{if !empty($item->Price45)}45 min: {$item->Price45}{/if}<br>
{if !empty($item->Price60)}60 min: {$item->Price60} | {/if}{if !empty($item->Price90)}90 min: {$item->Price90}{/if}</div>
<div class="NewsSummarySummaryHome">
{$item->Content|strip_tags|truncate:170:"...":true}
</div>
<div class="right newssumbutton">
<a class="button right" title="{$item->title|cms_escape:htmlall}" href="{$item->url}">Read more &gt;</a>
</div>
</div>

<!-- End News Display Template -->